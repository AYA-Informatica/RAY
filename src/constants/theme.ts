import { colors } from "@/design/colors";

/** Re-export of the colour tokens for non-Tailwind contexts (canvas, charts, meta). */
export { colors };
export const BRAND = {
  name: "RAY",
  tagline: "Buy & Sell Anything Near You",
  positioning:
    "A modern hyperlocal marketplace for East Africa where people buy, sell, rent, and discover trusted deals nearby.",
} as const;
